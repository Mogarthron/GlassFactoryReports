<UserControl x:Class="QualityControllDesktop.Views.QualityButtonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QualityControllDesktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="80">
    <UserControl.Resources>
        <Style TargetType="Button" x:Key="QuaBtn">            
            <Setter Property="Margin" Value="1,1,1,1"/>
        </Style>
    </UserControl.Resources>
    <Button x:Name="BtnBody"  Style="{StaticResource QuaBtn}">
        <StackPanel x:Name="SPLabels" Orientation="Vertical">
            
        </StackPanel>
    </Button>
</UserControl>
